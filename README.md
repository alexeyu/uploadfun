# uploadfun

A headless, config-driven, concurrent fan-out file uploader to multiple
FTP/FTPS/SFTP endpoints, built for unattended automation (cron, CI,
folder-watchers) rather than interactive use.

Status: under active development, not yet usable.
